# UV + Jupyter Setup History

```bash
uv init

uv venv

source .venv/bin/activate

uv add --dev jupyterlab ipykernel

uv run jupyter lab

uv pip install -r requirements.txt

uv run streamlit run app.py

uv run ruff format app.py