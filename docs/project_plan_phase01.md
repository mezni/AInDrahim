Here is the official Phase 1 Project Plan for the EV Charging Discovery Platform MVP. Following the Spec-Kit methodology, this plan translates your Architecture and Project Constitution into a highly actionable, 1-Week Sprint Task List.Phase 1 focuses entirely on Core Infrastructure Validation—setting up your local environment, configuring Docker containers, securing network perimeters, and testing database connections so you have a solid foundation for your Rust and frontend code.📅 Phase 1 Timeline Overview: Week 1 (Days 1–7)[Days 1-2: File Tree & Environment] ──► [Days 3-4: Container Composition] ──► [Days 5-7: Proxy & Access Control]
Objective: Bring the entire underlying Docker architecture up on a single host machine, verify isolated container networking, apply Nginx rate-limiting structures, and establish your core database schemas.Success Criteria: Running docker compose up -d launches all six core services cleanly, and external connection attempts to port 5432, 27017, and 5672 are dropped, while ports 80/443 successfully proxy traffic.📋 Comprehensive Phase 1 Task ListTask Block 1: Repository Structure & Environment Baseline (Days 1–2)[ ] 1.1 Initialize Workspace Directory Tree: Build out the unified multi-runtime project repository file structure on the host machine.Plaintexteverest-platform/
├── .github/workflows/     # CI/CD Action pipelines
├── services/
│   ├── backend/           # Rust Actix-web source workspace
│   └── worker/            # Rust asynchronous telemetry consumer
├── web-admin/             # React SPA Admin source code
├── mobile-driver/         # React Native source code
└── infra/
    ├── nginx/             # nginx.conf and SSL certificate assets
    ├── postgres/          # PostGIS initialization scripts (.sql)
    └── docker-compose.yml # Master on-premises orchestration script
[ ] 1.2 Create Master Security Environment Configuration File: Generate the root .env template file containing cryptographically strong, explicit passwords for internal databases, entirely avoiding default or placeholder credentials.[ ] 1.3 Configure Git Safeguards: Establish comprehensive root .gitignore parameters to prevent accidental commits of local .env values, compilation targets (/target, /node_modules), or persistent database storage folders (/infra/data).Task Block 2: Infrastructure & Container Composition (Days 3–4)[ ] 2.1 Draft the Master Composition Spec: Write the production-grade docker-compose.yml file mapping out all six operational images: postgis/postgis, mongo, rabbitmq:3-management-alpine, quay.io/keycloak/keycloak, your local backend stub, and nginx:alpine.[ ] 2.2 Enforce Network Perimeter Isolation: Strip out host-level port binding declarations (ports:) from the PostgreSQL, MongoDB, RabbitMQ, and Keycloak containers. Re-route them strictly under container-level visibility rules (expose:) hooked to a custom, private virtual bridge network (internal_network).[ ] 2.3 Provision Persistent Local Engine Volumes: Configure host-bound persistent storage mounts (volumes:) for database engines (pgdata, mongo_data), message broker queues (rabbitmq_data), and static assets (shared_media), ensuring data survives container upgrades.[ ] 2.4 Execute Initialization Database Schema Hooks: Write a custom init script (01_init.sql) inside the Postgres initialization hook folder (/docker-entrypoint-initdb.d/) to automatically pull the PostGIS spatial extensions, create your app databases, and set up distinct application pool users (RO_USER, RW_USER).Task Block 3: Edge Ingress Routing & Identity Realms (Days 5–7)[ ] 3.1 Build the Ingress Engine Configuration: Write the nginx.conf traffic distribution mapping, setting up dedicated limit_req_zone buckets for discovery endpoints (15r/s), telemetry routes (30r/s), and admin portals (5r/s).[ ] 3.2 Setup the React Single Page App Mount Point: Map out a static local file directory path straight into Nginx's system context under /admin/ with an internal fallback routing pattern (try_files $uri $uri/ /admin/index.html) to support client-side React routing.[ ] 3.3 Initialize Keycloak Access Realms: Spin up the identity system, log into the master dashboard, and configure the dedicated EV-Platform security realm. Set up separate authentication clients for the React Native mobile driver app (Public OpenID Connect) and the React SPA Admin panel (Confidential BFF target).[ ] 3.4 Perform End-to-End Environment Sanity Tests: Run a network assessment script using standard diagnostic tools (curl, nc) to verify that the host machine can only access ports 80 and 443, while proving Nginx can successfully talk to every internal backend component over the private bridge network.📊 Phase 1 Deliverables & Verification MatrixUse this checklist to verify that Phase 1 is completely healthy before writing any application code next week:Target ComponentVerification ActionExpected Healthy OutcomeEdge NetworkRun curl -I http://localhost/api/v1/public/stationsReturns an Nginx response code (502 Bad Gateway or 404 Not Found from the backend stub, not a connection refused error).Port ShieldingFrom an external machine, run nc -zv <host-ip> 5432Connection timeout or connection refused (proving the operational database is hidden).Spatial EngineConnect to PostgreSQL internally and run SELECT PostGIS_Version();Returns the active PostGIS extension version string successfully.Broker EngineAccess the internal RabbitMQ engine check route on port 15672Returns a valid HTTP 200 status code from the management dashboard instance.Identity EngineRun curl -I http://localhost:8080/auth/ internallyConfirms the Keycloak login workflow container is listening and healthy.